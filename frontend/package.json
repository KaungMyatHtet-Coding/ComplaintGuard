{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync:evaluation": "node scripts/sync-model-evaluation.mjs",
    "predev": "npm run sync:evaluation",
    "dev": "next dev",
    "prebuild": "npm run sync:evaluation",
    "build": "next build",
    "start": "next start",
    "prelint": "npm run sync:evaluation",
    "lint": "eslint",
    "pretypecheck": "npm run sync:evaluation",
    "typecheck": "tsc --noEmit --incremental false",
    "pretest": "npm run sync:evaluation",
    "test": "vitest run",
    "test:e2e": "playwright test --config playwright.config.ts"
  },
  "dependencies": {
    "firebase": "^12.16.0",
    "next": "16.2.10",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.10",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}

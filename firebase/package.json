{
  "name": "complaintguard-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test": "powershell -NoProfile -ExecutionPolicy Bypass -File ./run-emulator-tests.ps1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0",
    "vitest": "^4.0.0"
  }
}
